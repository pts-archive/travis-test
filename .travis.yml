os: linux
dist: xenial
language: php
php:
  - '7.0'
  - '7.1'
  - '7.2'
  - '7.3'
  - '7.4'

notifications:
    on_success: never
    on_failure: always

cache:
 directories:
   - $HOME/.composer/cache/files

install: composer install

script:
  - vendor/bin/phpunit --testsuite alltest
